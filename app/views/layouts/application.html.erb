<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <%# ヘッダーナビバー --------------------------------------------------------------------------------%>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
       
        <div class="container">
          <a class ="navbar-brand" href="/">
           <span>Bookers</span>
          </a>
          <%# ドロップダウンを設定 %>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
           </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            
          <ul class="navbar-nav ml-auto">
            <% if user_signed_in? %>
              <li>
                <a class="nav-link text-light" href="<%= user_path(current_user.id) %>" >
                  "Home"
                </a>
              </li>

              <li>
                <a class="nav-link text-light" href="/users">
                  "Users"
                </a>  
              </li>

              <li>
                <a class="nav-link text-light" href="/books">
                  "Books"
                </a> 
              </li>
              <%# メソッドを指定したいならこうやって書く %>
              <li>
                <%= link_to  destroy_user_session_path, method: :delete, class:"nav-link text-light" do%>
                  "Log out"
                <%end%>
                
                
              </li>
            <% else %>
              <li>
                <a class="nav-link text-light" href="/" >
                  "Home"
                </a>
              </li>
              <li>
                <a class="nav-link text-light" href="/homes/about" >
                  "About"
                </a>
              </li>

              <li>
                <a class="nav-link text-light" href="<%= new_user_registration_path %>" >
                  "Sign up"
                </a>
              </li>
              <li>
                <a class="nav-link text-light" href="<%= new_user_session_path %>" >
                  "Log in"
                </a>
              </li>
            <% end %>
          </ul>
          </div>
        </div>
        
      </nav>
    </header>

    <%# フラッシュメッセージ表示 %>
    <p id ="notice"><%= flash[:notice] %></p>
    <p id ="alert"><%= flash[:alert] %></p>

    <%# エラー表示  変数：error%>
    <% if error.errors.any? %>
      <%= error.errors.count %>errors prohibited this obj from being saved:
      <ul>
        <% error.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    <% end %>


    <%= yield %>

    <footer>
      <div class="container">
        <div class="row">
          <div class="col text-center">
            CopyRight Infratop.inc
          </div>
        </div>
      </div>
    </footer>

  </body>
</html>
